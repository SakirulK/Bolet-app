import type { AnchorHTMLAttributes } from "react";
/** Document navigation lets the service worker serve the offline shell reliably. */
export function LocalLink(props: AnchorHTMLAttributes<HTMLAnchorElement>) { return <a {...props} />; }
